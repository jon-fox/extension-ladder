---
kind: Service
apiVersion: v1
metadata:
  name: extension-ladder-service
spec:
  type: ClusterIP
  selector:
    app: extension-ladder
  ports:
  - name: http
    port: {{ .Values.ingress.PORT }}
    protocol: TCP
    targetPort: {{ .Values.env.PORT }}
